{
  "name": "aram-guide-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "start": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "deploy": "bash ./deploy/deploy-local.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
